import {colors} from './colors';
import {fontFamilies} from './fontFamilies';
import {border, fontSize, size} from './size';

const theme = {
  colors,
  fontFamilies,
  border,
  fontSize,
  size,
};

export default theme;
